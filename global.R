
#my_key <- "<mybucketkey>"
#my_secret <- "<mybucket_secretkey>"

#billing_bucket <- "<logging_bucket_name>"
#data_bucket <- "<datalog_bucket_name>"

#envtag <- "<environment_tag>" # when cleaning up tags in data import
# uses this tag to substitute for missing values (see dataprep.R CleanTags function)
# this tag (should be used in production for majority of services)

#commonsName <- "<sponsor_commons_name>" # string variable used in top left of app
#img <- "<image_url>" # url for image to used in top right of app.  horizontal rectangle preferred.
